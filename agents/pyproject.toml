[project]
name = "strands-knocks"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "strands-agents[a2a,anthropic,litellm,ollama,openai]>=1.42.0",
    "strands-agents-tools>=0.8.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
